version: '2'

services:
    backbone:
        extends:
            file: composer/third-party/backbone.yml
            service:
                 - db
                 - es
                 - mongo
                 - swift
    ck-service:
         extends:
             file: composer/third-party/ck-services.yml
             service:
                 - authservice
                 - knowledgecenter
                 - userpi

